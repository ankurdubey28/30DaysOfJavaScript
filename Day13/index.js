const {sum,person}=require('./Module')
const mod=require('./Module')
console.log(sum(4,5))
console.log(person)

console.log(mod.name)
mod.fun()

// Note to use require syntax set type to commonJS in package.json